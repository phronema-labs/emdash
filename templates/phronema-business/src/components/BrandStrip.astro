---
const brands = [
	"American Bar Association", "State Bar", "Martindale-Hubbell",
	"Best Lawyers in America", "Super Lawyers", "ACTEC"
];
---

<section class="brand-strip">
	<div class="container">
		<p class="brand-label">Professional memberships & recognition</p>
		<div class="brand-logos">
			{brands.map((brand) => (
				<span class="brand-name">{brand}</span>
			))}
		</div>
	</div>
</section>

<style>
	.brand-strip {
		padding: var(--space-2xl) var(--site-gutter);
		border-bottom: 1px solid var(--color-border);
	}

	.brand-label {
		text-align: center;
		font-size: var(--font-size-xs);
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: var(--color-muted);
		margin-bottom: var(--spacing-lg);
	}

	.brand-logos {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: var(--spacing-lg) var(--spacing-2xl);
	}

	.brand-name {
		font-size: var(--font-size-base);
		font-weight: 700;
		color: var(--color-text);
		opacity: 0.2;
		letter-spacing: 0.04em;
		text-transform: uppercase;
	}

	@media (max-width: 600px) {
		.brand-logos {
			gap: var(--spacing-md) var(--spacing-lg);
		}

		.brand-name {
			font-size: var(--font-size-sm);
		}
	}
</style>
